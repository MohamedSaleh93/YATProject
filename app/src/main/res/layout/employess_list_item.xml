<?xml version="1.0" encoding="utf-8"?>
<androidx.constraintlayout.widget.ConstraintLayout xmlns:android="http://schemas.android.com/apk/res/android"
    android:layout_width="match_parent"
    android:layout_height="wrap_content"
    xmlns:app="http://schemas.android.com/apk/res-auto">
    
    <ImageView
        android:id="@+id/employeeAvatar"
        android:layout_width="@dimen/employee_image_list_item_size"
        android:layout_height="@dimen/employee_image_list_item_size"
        app:layout_constraintStart_toStartOf="parent"
        app:layout_constraintTop_toTopOf="parent"
        app:layout_constraintBottom_toBottomOf="parent"
        android:layout_margin="@dimen/employee_image_margin"
        android:src="@drawable/ic_default_avatar"/>

    <TextView
        android:id="@+id/employeeName"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        app:layout_constraintStart_toEndOf="@id/employeeAvatar"
        app:layout_constraintTop_toTopOf="parent"
        app:layout_constraintBottom_toBottomOf="parent"
        android:textColor="@android:color/holo_red_dark"
        android:layout_marginStart="@dimen/margin_start"
        android:textStyle="bold"
        android:textSize="@dimen/employee_name_size"/>

</androidx.constraintlayout.widget.ConstraintLayout>